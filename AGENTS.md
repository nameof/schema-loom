对本项目的任务，无论何时遵循以下原则：
- 除非用户明确指出编写或修改代码，否则在编写或修改代码之前必须进行询问，禁止自行更改
- 当开发后端java代码时，禁止自行编写任何冗余的工具类代码或使用原始java api，例如操作文件、IO、字符串、时间日期等等。 也不能使用apache等传统老旧util，必须从Hutool中寻找util调用
- 有任何未知问题或风险确认必须与用户进行交互式确认，禁止静默忽略不合理设计和风险，以及长篇大论输出结果让用户阅读，必须是交互式
- 涉及到老旧代码复用、重构时，不能无脑套用，需评估过时、不优雅、不合理、潜在风险设计和BUG，需要主动询问用户是否需要重构