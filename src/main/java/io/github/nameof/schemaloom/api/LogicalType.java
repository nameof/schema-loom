package io.github.nameof.schemaloom.api;

public enum LogicalType {
    BOOLEAN, INT16, INT32, INT64, DECIMAL, FLOAT32, FLOAT64, STRING,
    DATE, TIME, TIMESTAMP, BINARY, OFFSET_TIME, OFFSET_TIMESTAMP
}
